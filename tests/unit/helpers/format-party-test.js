import { formatParty } from 'bill-tracker/helpers/format-party';
import { module, test } from 'qunit';

module('Unit | Helper | format party');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatParty([42]);
  assert.ok(result);
});
